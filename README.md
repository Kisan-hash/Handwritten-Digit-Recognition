# Handwritten-Digit-Recognition
The Handwritten Digit Recognition System is a deep learning-based image classification model that identifies handwritten digits (0–9) using the MNIST dataset.  The system uses a Convolutional Neural Network (CNN) to extract features from grayscale images and classify them into 10 digit categories.
